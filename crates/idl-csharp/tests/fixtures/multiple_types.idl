// Fixture: multiple top-level types in one header.
enum Status {
    ACTIVE,
    INACTIVE
};

typedef long EntityId;

struct Entity {
    EntityId id;
    Status state;
};

exception EntityNotFound {
    EntityId missing_id;
};
