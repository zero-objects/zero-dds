// Fixture: multiple top-level types → multiple Java files.
enum Status {
    ACTIVE,
    INACTIVE
};

typedef long EntityId;

struct Entity {
    EntityId id;
    Status state;
};

exception EntityNotFound {
    EntityId missing_id;
};
