# OpenDDS TCP-Transport-Variante fuer TCP-cross-vendor-Bench.
# Discovery weiter RTPS-Multicast. Data via tcp-transport.
[common]
DCPSGlobalTransportConfig=$file
DCPSDefaultDiscovery=rtps_disc

[rtps_discovery/rtps_disc]
use_xtypes=no

[transport/the_tcp_transport]
transport_type=tcp
