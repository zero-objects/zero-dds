# arm-host Cyclone DDS bench env (host: user@arm-host).
# Cyclone install: ~/opt/cyclone via source build (11.0.1).

export DYLD_LIBRARY_PATH="$HOME/opt/cyclone/lib${DYLD_LIBRARY_PATH:+:$DYLD_LIBRARY_PATH}"
