# arm-host Fast-DDS bench env (host: user@arm-host).
# Fast-DDS install: ~/opt/fastdds via source build (v3.6.1).
# Includes libfastdds, libfastcdr, libfoonathan_memory (static).

export DYLD_LIBRARY_PATH="$HOME/opt/fastdds/lib${DYLD_LIBRARY_PATH:+:$DYLD_LIBRARY_PATH}"
