# x86-host ZeroDDS bench env (host: root@x86-host, Debian 13).
# ZeroDDS source build: $ZERODDS_REPO, libzerodds.so im
# target/release.

export LD_LIBRARY_PATH="$ZERODDS_REPO/target/release${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
