# gRPC over HTTP/2 — Open + Partial Items

— keine offenen Items.

Custom-Metadata Binary/ASCII-Encoding, gRPC-Web Base64-Subprotocol,
sowie alle Required-Header-Konstanten sind in
`crates/grpc-bridge/src/metadata.rs` implementiert.
