# IDL-to-C# 1.0 — Open Items

— no open items.

Annex A.1 CORBA-specific mappings are implemented as an opt-in codegen
backend (`CsGenOptions::emit_corba_traits` /
`generate_csharp_with_corba_traits`); see the main file §A.1.
