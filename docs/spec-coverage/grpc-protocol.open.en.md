# gRPC over HTTP/2 — Open + Partial Items

— no open items.

Custom-metadata binary/ASCII encoding, the gRPC-Web Base64 subprotocol,
and all required header constants are implemented in
`crates/grpc-bridge/src/metadata.rs`.
