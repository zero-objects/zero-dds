# OMG Web-Enabled DDS 1.0 — Open + Partial Items

Aggregat aus `zerodds-web-1.0.md`. Nicht von Hand pflegen — vor jedem
Audit-Lauf löschen und aus dem Hauptfile neu generieren.

## Open

— keine.

## Partial

— keine.

## Decision-Records (`n/a (rejected)`)

— keine.

---

Voll abgedeckt: REST-PSM via `crates/web/`, SOAP-PSM via
`crates/zerodds-soap/`.
