# zerodds-xcdr2-java-1.0 -- open items + decision records

## §9 L4 Cross-Vendor Cyclone-recorded Fixtures

**Status:** `partial` -- skeleton `tests/interop/xcdr2_cross_vendor.sh` + 14 spec-derived + V-2 real Cyclone-DDS-0.10.2 capture via tcpdump on the Linux bench host. Decoder round-trip test green. Remaining open: V-3..V-12 as real Cyclone captures (same pipeline on the Linux bench host).
