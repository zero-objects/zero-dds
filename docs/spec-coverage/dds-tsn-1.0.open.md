# DDS-TSN 1.0 — Open + Partial Items

Aggregat aus `dds-tsn-1.0.md`. Nicht von Hand pflegen — vor jedem
Audit-Lauf löschen und aus dem Hauptfile neu generieren.

## Open

— keine.

## Partial

### §7.3 Configuration Representation (PSM)

**Status:** `partial` — XML + JSON-PSM via
`crates/transport-tsn/src/pim/{xml,json}.rs` done; YANG-PSM
(RFC 7950) nicht implementiert. Aufwand 0.5-1 PW falls verlangt.

## Decision-Records (`n/a (rejected)`)

— keine.
