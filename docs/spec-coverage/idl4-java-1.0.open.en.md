# IDL-to-Java 1.0 — Open Items

— no open items.

Annex A.1 CORBA-specific mappings are implemented as an opt-in codegen
backend (`JavaGenOptions::emit_corba_traits` /
`generate_java_files_with_corba_traits`); see the main file §A.1.
