# DDS X-Types 1.3 — Open + Partial Items

Aggregat aus `dds-xtypes-1.3.md`. Nicht von Hand pflegen — vor jedem
Audit-Lauf löschen und aus dem Hauptfile neu generieren.

## Open

— keine.

## Partial

— keine.

## Decision-Records (`n/a (rejected)`)

— keine.

---

XTypes 1.3 voll abgedeckt: TypeObject + TypeLookup + Assignability +
IDL-Semantik via `crates/types/`, XML Data Representation Profile via
`crates/zerodds-xml-wire/`.
