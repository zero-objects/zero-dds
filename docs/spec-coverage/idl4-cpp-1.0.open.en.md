# IDL-to-C++ 1.0 — Open Items

— no open items.

Annex A.1 CORBA-specific mappings are implemented as an opt-in codegen
backend (`CppGenOptions::emit_corba_traits` /
`generate_cpp_header_with_corba_traits`); see the main file §A.1.0.
