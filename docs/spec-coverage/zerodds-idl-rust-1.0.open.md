# zerodds-idl-rust 1.0 — Open + Decision-Records

Aggregat aus `zerodds-idl-rust-1.0.md`.

**Stand: keine offenen Items.** Alle ehemaligen partials/opens/n-a-rejected wurden in Tier 1 + Tier 2 + Tier 3 (Architektur-Trennung via `corba-rust`-Crate) auf done gebracht.

CORBA-spezifische Konstrukte (Valuetype/Interface/Component/Home — §7.5) werden im separaten `zerodds-corba-rust`-Crate behandelt; Coverage dafuer in `zerodds-corba-rust-1.0.md` und `zerodds-corba-rust-1.0.open.md`.
